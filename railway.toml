[build]
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Volume: mount /app/data in Railway dashboard for SQLite persistence
# Set SPO2_DB_PATH=/app/data/spo2.db (already the default)
